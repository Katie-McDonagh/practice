// function oppositeNumber(number){

//   return number > 0 ?  -Math.abs(number) : Math.abs(number);
// }

// questions to answer:


//alternative

function oppositeNumber(number){
  return number - (number * 2)
}